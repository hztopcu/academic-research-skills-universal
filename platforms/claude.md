# Claude Code

Status: stable

```bash
ars claude install
ars claude verify
```

The Claude adapter installs the four upstream skills as separate skill folders and places shared commands, scripts, and references next to them.

This is the closest layout to the upstream skill model.
