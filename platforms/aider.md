# Aider

Status: stable

```bash
ars aider install
ars aider verify
```

The Aider adapter currently uses the portable bundle layout. Load the root `SKILL.md` or include the relevant skill package instructions in your Aider context.

The next improvement is an Aider-specific include file if we want tighter workflow ergonomics.
